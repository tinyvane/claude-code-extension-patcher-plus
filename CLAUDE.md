# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This is currently an empty repository initialized with Claude Code and SpecStory extension configurations. The repository name suggests it may be related to permission bypass functionality or reverting such changes.

## Project Structure

Currently, the repository contains only:
- `.gitignore` - Git ignore rules for Claude Code chats and local settings
- `.claude/` - Claude Code configuration directory
- `.specstory/` - SpecStory extension artifacts for AI chat history

## Development Setup

No build system, package manager, or dependencies have been configured yet.

## Notes for Future Development

When code is added to this repository, update this file with:
- Build commands (e.g., `npm run build`, `make`, etc.)
- Test commands and how to run specific tests
- Linting/formatting commands
- High-level architecture overview
- Key design decisions and patterns used
- Integration points and dependencies between modules
