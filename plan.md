# bypassPermissions Revert - Claude Code 权限补丁（回滚版）- 项目规划

## 项目概述
Claude Code 权限补丁工具的回滚版本（含 `ultra-yolo-patcher.py` 和 `ARCHITECTURE.md`），与 `claude-code-extension-patcher-plus` 相同。

---

## 2026-03-03 Claude 建议

### 版本关系
此项目与 `claude-code-extension-patcher-plus` 内容相同，是同一工具在不同时间的版本（此版本创建于2025年10月14日）。

### 建议
- **保留**：作为权限补丁工具的历史版本参考
- **主版本**：使用 `claude-code-extension-patcher-plus` 作为主维护版本
- 了解此版本与主版本的差异（可能有 bug fix 或功能变化）

完整规划建议请参考 `claude-code-extension-patcher-plus/plan.md`。
